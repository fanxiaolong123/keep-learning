---
name: metalearn-map
description: "Run MetaLearn Step 1 and Step 2 to define a learning goal, assess the user's starting point, research the topic, build a verified knowledge map, and create the Learn Plan. Use when the user explicitly invokes metalearn-map, asks for MetaLearn map, step1, step2, positioning, or knowledge mapping."
---

# MetaLearn Map

Read `references/metalearn-map.md` completely before acting and follow it as written.

- For a new topic, `map`, or `step1`, begin with Step 1.
- For `step2`, enter Step 2 directly using the established Step 1 context.
- Accept equivalent Chinese step names and spacing variants.
- When HTML is selected, read and use `references/metalearn-map-template.html` as required by the source.
- Use the available web-search or browsing capability when the source says `WebSearch`.
- Create or update `Learn Plan - [xxx]` in the active workspace when required.

Do not omit, reorder, summarize, or silently correct the source workflow.
